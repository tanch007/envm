version: '1.0'
name: pipeline-20260604
displayName: pipeline-20260604
triggers:
  trigger: auto
  push:
    branches:
      prefix:
        - master
stages:
  - name: stage-67d04326
    displayName: build-ui
    strategy: naturally
    trigger: auto
    executor: []
    steps:
      - step: build@nodejs
        name: build_nodejs
        displayName: build-ui
        nodeVersion: 24.5.0
        commands:
          - cd ui
          - '# 设置NPM源，提升安装速度'
          - npm config set registry https://registry.npmmirror.com
          - ''
          - '# 执行编译命令'
          - npm install
        artifacts:
          - name: BUILD_ARTIFACT
            path:
              - ./dist
        caches:
          - ~/.npm
          - ~/.yarn
        notify: []
        strategy:
          retry: '0'
  - name: stage-6b5e4f58
    displayName: build-main
    strategy: naturally
    trigger: auto
    executor: []
    steps:
      - step: build@nodejs
        name: build_nodejs
        displayName: Nodejs 构建
        nodeVersion: 24.5.0
        commands:
          - cd app
          - ''
          - '# 设置NPM源，提升安装速度'
          - npm config set registry https://registry.npmmirror.com
          - ''
          - '# 执行编译命令'
          - npm install && npm run build:winui
        artifacts:
          - name: BUILD_ARTIFACT
            path:
              - ./dist/release
        caches:
          - ~/.npm
          - ~/.yarn
        notify: []
        strategy:
          retry: '0'
